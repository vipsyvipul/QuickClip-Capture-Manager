import { App, MarkdownPostProcessorContext, TFile } from 'obsidian'

export function processFullPage(_app: App, _el: HTMLElement, _ctx: MarkdownPostProcessorContext): void {
    // reserved for future full-page rendering
}

export function injectFullPageHeader(_app: App, _container: HTMLElement, _sourcePath: string): void {
    // reserved for future full-page rendering
}
